@model bazar_prg.Models.Cliente

@{
    ViewData["Title"] = "EDITAR";
}

<h1>EDITAR ESTADO DE ATENCIÓN</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="Name" />
            <input type="hidden" asp-for="Surname" />
            <input type="hidden" asp-for="Email" />


            <div class="form-group">
                <label class="control-label">ESTADO DE LA ATENCIÓN</label>
                
                 <select asp-for="Estado" class="form-control">
                 <option value="PENDIENTE POR LLAMAR">PENDIENTE POR LLAMAR</option>
                 <option value="ATENDIDO" >ATENDIDO</option>
                 </select>
               
                <span aspvalidation-for="Estado" class="text-danger"></span>
            </div>
            <br> 

            <div class="form-group">
                <input type="submit" value="GUARDAR CAMBIO" class="btn btn-primary" />
            </div>
        </form>
    </div>

      <a class="btn btn-primary" style="background-color: #3b5998;"  asp-action="Listado" role="button">
                <img src="https://cdn-icons-png.flaticon.com/512/570/570220.png" width="25px" heigh="25px"></i> Regresar</a>
</div>

<div>
  
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
