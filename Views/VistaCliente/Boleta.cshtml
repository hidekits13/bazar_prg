@model dynamic;
@using System.Collections.Generic;

@{
    ViewData["Title"] = "Details";
     IEnumerable<bazar_prg.Models.DetallePedido> listItemsCarrito = new List<DetallePedido>();
        listItemsCarrito = @Model.elementosCarrito;

        int yer=0,tor=0,re=0;

}
<h1>Usuario</h1>
@foreach(DetallePedido item in Model.elementosCarrito) {

    if(yer<1){
    <h4>@Html.DisplayFor(modelItem => item.pedido.UserID)</h4>
        yer++;
    }

}
    <hr />

    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => listItemsCarrito.FirstOrDefault().Cantidad)
        </dt>
         @foreach(DetallePedido item in Model.elementosCarrito) {
            if(tor<1){
          <dd class = "col-sm-10">
           @Html.DisplayFor(modelItem => item.Cantidad)
        </dd>
        tor++;
            }
         }
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => listItemsCarrito.FirstOrDefault().pedido.Total)
        </dt>
        @foreach(DetallePedido item in Model.elementosCarrito) {
            if(re<1){
        <dd class = "col-sm-10">
            @Html.DisplayFor(modelItem => item.pedido.Total)
        </dd>
        re++;
            }
        }
    </dl>

<table class="table">
    <thead>
        <tr>
            <th>
               @Html.DisplayNameFor(model => listItemsCarrito.FirstOrDefault().Producto.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => listItemsCarrito.FirstOrDefault().Producto.Precio)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(DetallePedido item in Model.elementosCarrito) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Producto.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto.Precio)
            </td>
        </tr>
        }
    </tbody>
</table>