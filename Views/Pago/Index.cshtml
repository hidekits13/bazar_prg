@model IEnumerable<bazar_prg.Models.Pago>

@{
    ViewData["Title"] = "Index";
    var count = 1;
}
<div class="container">        
    <a class="btn btn-primary" style="background-color: #3b5998;"asp-action="ExportarExcel" role="button">
                <img src="https://cdn-icons-png.flaticon.com/512/2921/2921797.png" width="25px" heigh="25px"></i> GENERAR REPORTE EXCEL</a>
</div>

<table class="table table-hover">
    <thead class="thead-dark">
        <tr>
            <th scope="col">
                        №
             </th>
             <th scope="col">
                Correo Cliente
             </th>
             <th scope="col">
                  Fecha de Pago
             </th>
                    
              <th scope="col">
                 @Html.DisplayNameFor(model => model.MontoTotal)
              </th>
              <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
           <th scope="row">
                    @count
            </th>
            <td>
                 @Html.DisplayFor(modelItem => item.UserID)
            </td>
            <td>
               @Html.DisplayFor(modelItem => item.PaymentDate)
            </td>                
            <td>
                  S/. @Html.DisplayFor(modelItem => item.MontoTotal)
                </td>
            <td>             
                <!--
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger">Eliminar</a>
                -->
            </td>
        </tr>
        count++;
}
    </tbody>
</table>
<br>
<br>
